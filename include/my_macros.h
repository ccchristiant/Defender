/*
** EPITECH PROJECT, 2021
** my_macros.h
** File description:
** macros header
*/

#ifndef MACROS_H_
#define MACROS_H_

#define CTOI(x) (x - '0')

#define ITOC(x) (x + '0')

#define ABS(x)  ((x > 0) ? (x) : (-x))

static const int MAX_ID = 6;

static const int WIDTH = 1920;

static const int HEIGHT = 1080;

static const int NB_SCENES = 8;

static const int PRICES[5] = {20, 40, 50, 70, 80};

#endif